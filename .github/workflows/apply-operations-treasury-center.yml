name: Apply operations treasury center

on:
  push:
    branches: [agent/operations-treasury-center]
    paths:
      - '.github/scripts/apply-operations-treasury-center.mjs'
      - '.github/workflows/apply-operations-treasury-center.yml'
      - 'src/pages/OperationsTreasuryCenter.jsx'

permissions:
  contents: write

jobs:
  patch-and-build:
    if: ${{ !contains(github.event.head_commit.message, 'apply operations treasury center') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/operations-treasury-center
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply integration patch
        run: node .github/scripts/apply-operations-treasury-center.mjs
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/components/layout/AppLayout.jsx
          git commit -m "feat: apply operations treasury center" || exit 0
          git push origin HEAD:agent/operations-treasury-center
