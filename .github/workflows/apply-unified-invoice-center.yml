name: Apply unified invoice center

on:
  push:
    branches: [agent/unified-invoice-center]
    paths:
      - '.github/scripts/apply-unified-invoice-center.mjs'
      - '.github/workflows/apply-unified-invoice-center.yml'
      - 'src/pages/UnifiedInvoiceCenter.jsx'

permissions:
  contents: write

jobs:
  patch-and-build:
    if: ${{ !contains(github.event.head_commit.message, 'apply unified invoice center') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/unified-invoice-center
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply integration patch
        run: node .github/scripts/apply-unified-invoice-center.mjs
      - name: Install dependencies
        run: npm ci
      - name: Build application
        run: npm run build
      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/components/layout/AppLayout.jsx
          git commit -m "feat: apply unified invoice center" || exit 0
          git push origin HEAD:agent/unified-invoice-center
