name: Apply daily tasks center

on:
  push:
    branches: [agent/daily-tasks-center]
    paths:
      - '.github/scripts/apply-daily-tasks-center.mjs'
      - '.github/workflows/apply-daily-tasks-center.yml'
      - 'src/pages/DailyTasksCenter.jsx'

permissions:
  contents: write

jobs:
  patch-and-build:
    if: ${{ !contains(github.event.head_commit.message, 'apply daily tasks center') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/daily-tasks-center
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply integration patch
        run: node .github/scripts/apply-daily-tasks-center.mjs
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/components/layout/AppLayout.jsx
          git commit -m "feat: apply daily tasks center" || exit 0
          git push origin HEAD:agent/daily-tasks-center

# Verification workflow for the read-only daily tasks center.
