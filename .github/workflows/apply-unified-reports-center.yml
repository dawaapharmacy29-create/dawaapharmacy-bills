name: Apply unified reports center

on:
  push:
    branches: [agent/unified-reports-center]
    paths:
      - '.github/scripts/apply-unified-reports-center.mjs'
      - '.github/workflows/apply-unified-reports-center.yml'
      - 'src/pages/UnifiedReportsCenter.jsx'

permissions:
  contents: write

jobs:
  patch-and-build:
    if: ${{ !contains(github.event.head_commit.message, 'apply unified reports center') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/unified-reports-center
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply integration patch
        run: node .github/scripts/apply-unified-reports-center.mjs
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/components/layout/AppLayout.jsx
          git commit -m "feat: apply unified reports center" || exit 0
          git push origin HEAD:agent/unified-reports-center

# navigation verification trigger
