# حزمة حماية البيانات الأولى

تم تنفيذ التعديلات التالية على نسخة منفصلة من مشروع dawaawael:

- منع فاتورة شراء بتاريخ مستقبلي.
- منع تكرار رقم فاتورة المورد لنفس المورد والفرع.
- منع تكرار تسليم نفس الفرع والشيفت والتاريخ.
- إلزام سبب واضح لكل صنف في المرتجع مع توسيع قائمة الأسباب.
- إلزام طلب الصيدلية بالفرع والموظف المسؤول ورقم موبايل مصري صحيح.
- إلزام طلب إعادة التوريد باسم الصنف والكود والفرع والكمية.
- منع إنشاء عضو فريق بنفس الاسم بعد التطبيع.
- إلزام وظيفة عضو الفريق وتوحيد ترتيب الفروع.
- إيقاف الحذف المباشر لأعضاء الفريق لحماية العلاقات القديمة.
- إضافة أدوات موحدة للتحقق من البيانات في src/lib/dataIntegrity.js.

## التحقق

تعذر إكمال npm install / npm run build داخل بيئة التنفيذ بسبب تجاوز مهلة تثبيت الحزم، لذلك هذه نسخة تطوير وليست نسخة إنتاج منشورة. يجب تشغيل:

```bash
npm install
npm run build
npm run lint
```

قبل النشر على Base44.

## ما لم يُنفذ تلقائيا

لم يتم تعديل بيانات Base44 الحية أو حذف السجلات القديمة. دمج د أحمد حافظ وتسوية الموردين والدفعات يحتاجان اتصالا مباشرا ببيانات تطبيق Base44 أو تنفيذ أدوات مراجعة داخل التطبيق بعد نجاح البناء.

## المرحلة الثانية — مركز المراجعة والحماية المالية

- إضافة صفحة إدارية جديدة: `مركز مراجعة البيانات` على المسار `/data-review`.
- الصفحة تفحص بدون تعديل:
  - تكرارات فريق العمل.
  - الموظفين ناقصي الوظيفة أو الفرع.
  - الموردين ناقصي التصنيف أو طريقة الدفع.
  - دفعات الموردين بدون فرع.
  - الدفعات المسجلة على الفروع الداخلية.
  - فواتير المورد المحتملة التكرار.
  - المرتجعات المفتوحة أو ناقصة الأسباب.
  - تسليمات الشيفت المحتملة التكرار.
  - طلبات العملاء القديمة والناقصة.
  - طلبات إعادة التوريد ناقصة البيانات.
- إضافة تصدير Excel موحد بكل سجلات المراجعة.
- منع المورد المكرر بالاسم.
- منع حذف المورد مباشرة لحماية الفواتير والدفعات المرتبطة.
- المورد الداخلي يُستبعد تلقائيًا من صافي المشتريات ولا تُطبق عليه شروط دفع الموردين.
- منع إنشاء دفعة مورد بدون فرع.
- منع إدخال دفعة مورد على جهة مصنفة كفرع داخلي.
- استبعاد الفروع الداخلية من صفحة أرصدة الموردين الإجمالية.

### التحقق
تعذر إكمال تثبيت `node_modules` في بيئة التنفيذ الحالية؛ مجلد التثبيت أصبح جزئيًا وملف `vite` لم يُثبت. لذلك يجب تشغيل:

```bash
rm -rf node_modules
npm ci
npm run build
npm run lint
```

قبل النشر على Base44.


## المرحلة الثالثة – سلامة حساب الموردين
- منع احتساب الدفعات العامة بدون فرع على الفرعين في نفس الوقت.
- جعل سجل SupplierPayment هو المصدر المفضل لقيمة السداد المرتبطة بالفاتورة، مع fallback للـ paid_value القديم فقط للبيانات التاريخية.
- منع دفع مبلغ أكبر من المتبقي على الفاتورة.
- إيقاف تحديث paid_value يدويًا عند إنشاء دفعة جديدة لتجنب الازدواج.
- تمييز الدفعات العامة الجديدة بأنها غير موزعة وإظهارها في مركز مراجعة البيانات.
- إضافة أدوات حساب مركزية في src/lib/supplierFinancialIntegrity.js.

## المرحلة الرابعة — توزيع دفعات الموردين

- إضافة نافذة توزيع الدفعة العامة على فاتورة أو أكثر لنفس المورد والفرع.
- عرض الفواتير الآجلة المفتوحة من الأقدم إلى الأحدث.
- دعم التوزيع اليدوي والتوزيع التلقائي على أقدم الفواتير.
- منع تجاوز قيمة الدفعة أو المتبقي على أي فاتورة.
- إنشاء سجل دفع مرتبط بكل فاتورة مع مرجع للدفعة الأصلية.
- تخفيض رصيد الدفعة العامة بالمبلغ الموزع مع الاحتفاظ بسجل نصي للتوزيع.
- محاولة التراجع عن القيود الجديدة إذا فشل تحديث الدفعة الأصلية.
- إظهار حالة كل دفعة في سجل المدفوعات: مرتبطة، قابلة للتوزيع، أو تحتاج تحديد فرع.

### ملاحظة تشغيلية
لم يتم إنشاء كيان جديد في Base44، لذلك استخدمت نفس كيان `SupplierPayment` للحفاظ على التوافق مع البيانات الحالية. يجب تجربة التوزيع أولًا على نسخة تجريبية أو دفعة صغيرة قبل استخدامه على جميع الدفعات القديمة.

## المرحلة الخامسة
- إضافة صفحة مستقلة لتسويات وتحويلات الفروع باستخدام سجلات مميزة `payment_kind=branch_settlement`.
- استبعاد تسويات الفروع من شاشة مدفوعات الموردين ومن قائمة الدفعات غير الموزعة.
- إظهار الحركات القديمة المسجلة باسم فرع كقائمة تحتاج مراجعة، بدون تحويل تلقائي.
- إضافة توثيق إشعار الخصم للمرتجع المنفذ: الرقم، القيمة، التاريخ، الملاحظات، ومسؤول التسجيل.
- إضافة تنبيه في مركز مراجعة البيانات للمرتجعات المنفذة بدون إشعار خصم.
- إيقاف الحذف المباشر للمرتجعات للحفاظ على الأثر المالي وسجل المراجعة.

### تنبيه تشغيل
قد تحتاج حقول Base44 الجديدة (`payment_kind`, `source_branch`, `destination_branch`, `credit_note_*`) إلى إضافتها في مخطط الكيانات إذا كانت بيئة Base44 تمنع الحقول المرنة.


## المرحلة السادسة — مركز دورة المشتريات
- إضافة صفحة مركزية لدورة المشتريات تعرض نواقص لم تُطلب، طلبات لم تُستلم، فواتير تنتظر المراجعة، ومرتجعات غير مغلقة ماليًا.
- إضافة انتقال مباشر من الاختناقات إلى الشاشة المسؤولة.
- توسيع حالات طلبات إعادة التوريد إلى دورة كاملة من البحث وحتى الاستلام والإغلاق.
- إيقاف الحذف المباشر لطلبات إعادة التوريد واستخدام حالة «ملغي» بدلًا منه.
- إضافة ملخص تقديري لقيمة الفواتير المفتوحة.

## المرحلة السابعة: سلامة فريق العمل والدمج الآمن
- إضافة حالة الموظف والفرع الأساسي وتاريخ التعيين.
- إخفاء الموظفين المؤرشفين أو غير النشطين من قوائم الفروع النشطة.
- إضافة صفحة إدارية لمعاينة تكرارات الموظفين.
- معاينة عدد المراجع في طلبات العملاء والصيدليات والمرتجعات والمصروفات وتسليم الشيفت والجرد.
- نقل المراجع إلى السجل الأساسي ثم أرشفة السجل المكرر بدون حذف.
- تسجيل عملية الدمج في سجل العمليات.
- الحقول الجديدة المحتملة في TeamMember: primary_branch, employment_status, is_active, joined_at, notes, merged_into_id, merged_into_name, merged_at.
