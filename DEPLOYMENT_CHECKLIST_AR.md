# قائمة فحص النشر الآمن

## 1) نسخة الأمان

- صدّر جميع كيانات Base44 قبل رفع النسخة.
- احتفظ خصوصًا بـ: PurchaseInvoice, SupplierPayment, Supplier, Return, TeamMember, ShiftDelivery, PharmacyOrder, ReplenishmentOrder.

## 2) تثبيت وبناء

```bash
rm -rf node_modules dist
npm ci --no-audit --no-fund
npm run build
npm run lint
```

إذا تعذر `npm ci` بسبب اختلاف lockfile:

```bash
rm -rf node_modules
npm install
npm run build
npm run lint
```

## 3) تحديث الكيانات

ملفات المخطط المحدثة موجودة داخل `base44/entities/`. تأكد أن Base44 طبق الحقول الجديدة قبل تجربة العمليات المالية أو الدمج.

## 4) اختبار تجريبي

- استخدم دفعة مورد صغيرة أو بيانات تجريبية.
- لا تبدأ بدمج د أحمد حافظ مباشرة.
- راجع مركز البيانات أولًا وصدّر تقرير Excel.
- نفّذ دمجًا تجريبيًا على سجلين غير مستخدمين إن أمكن.

## 5) ترتيب تشغيل البيانات القديمة

1. تحديد وظائف وفروع الموظفين الناقصة.
2. مراجعة دفعات الموردين بدون فرع.
3. فصل الحركات القديمة المسجلة على الفروع الداخلية.
4. توزيع الدفعات العامة على الفواتير.
5. مراجعة الفواتير المحتملة التكرار.
6. توثيق إشعارات خصم المرتجعات القديمة.
7. مراجعة تسليمات الشيفت المكررة.
8. مراجعة طلبات العملاء والنواقص القديمة.
9. بعد ذلك فقط تنفيذ دمج الموظفين المؤكدين.

## 6) الرجوع للخلف

إذا ظهر خطأ مالي، أوقف إدخال دفعات جديدة وأعد النسخة السابقة من الكود. لا تحذف سجلات التوزيع يدويًا قبل مقارنة الدفعة الأصلية والفواتير المرتبطة.
